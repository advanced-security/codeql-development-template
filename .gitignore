*.swp
.DS_Store

node_modules

# Generated via `codeql-mcp-server-ci` actions workflow
models-output.json

# Test databases created via `codeql test run`
*.testproj
trap

# CodeQL pack lock files are generated locally based on the installed CodeQL CLI version
codeql-pack.lock.yml

