name: languages-python-tools-test
version: 0.0.2
dependencies:
  # This test pack does not actually depend upon `codeql/python-queries`,
  # but we declare the dependency to ensure that the queries from the
  # query pack are downloaded and available locally.
  codeql/python-queries: "*"
  languages-python-tools-src: ${workspace}
extractor: python
